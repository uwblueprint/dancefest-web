version: '2.1'
services:
  dancefest-backend:
    restart: always
    build: .
    ports:
      - 5000:5000
    volumes:
      - .:/dancefest-backend
    env_file:
        - .env
    depends_on:
      - db
  db:
    image: postgres:12-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ansonyuu
      - POSTGRES_DB=dancefest

volumes:
  postgres_data:
